# Isar version 13 fixture

`default.isar` was generated with the official Isar `3.1.0+1` packages and
the UniMate version-code-13 schema. It contains fixed representative records
for courses, past semesters with embedded course results, timetable entries,
and user preferences.

The compatibility test copies this file before opening it so the source
fixture is never modified.
